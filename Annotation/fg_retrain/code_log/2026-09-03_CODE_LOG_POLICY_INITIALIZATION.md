# 代码修改记录：启用强制日志制度

## 修改时间

2026-09-03

## 修改目的

为 `Annotation/fg_retrain` 建立长期可追踪的代码修改记录，保证E0、E1以及后续实验的实现、配置和运行方式能够随时复核与复现。

## 修改范围

仅新增代码日志目录及日志规范，不修改任何实验的数据流、训练逻辑、评测逻辑或配置参数。

## 修改文件及具体内容

### 新增 `Annotation/fg_retrain/code_log/README.md`

- 定义需要记录的文件范围；
- 定义日志文件命名方式；
- 规定每条日志必须包含的字段；
- 规定旧实验不得被新实验覆盖；
- 规定共享模块修改必须说明兼容性和验证结果。

### 新增 `Annotation/fg_retrain/code_log/2026-09-03_CODE_LOG_POLICY_INITIALIZATION.md`

- 记录本次日志制度初始化工作；
- 作为后续代码日志的格式示例和起点。

## 是否影响旧实验

不影响。E0和E1的Python入口、JSON配置、Shell启动脚本及计算逻辑均未改变。

## 验证方法与结果

- 确认两个日志文件位于独立的 `code_log` 目录；
- 本次没有修改可执行代码，因此不需要重新训练或重新评测模型。

## 对应运行命令

无。本次变更仅建立日志制度。

## 输出位置

```text
Annotation/fg_retrain/code_log/
```

## 已知假设或限制

本制度从2026-09-03起强制执行。此前已经完成的E0、E1开发内容保留在各自入口、配置、README及输出目录中，不在本次逐项倒推生成历史日志。

